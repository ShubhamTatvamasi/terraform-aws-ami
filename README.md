# terraform-aws-ami

Update your region in `main.tf` file.

Initalize Terraform:
```bash
terraform init
```

Get AMI ID:
```bash
terraform apply -auto-approve
```

Destroy data:
```bash
terraform destroy -auto-approve
```
